    <!-- 底部页脚 -->
    <footer>
        <div class="footer-content">
            <div class="footer-links">
                <a href="<%= typeof localeUrl !== 'undefined' ? localeUrl('/about') : '/about' %>" class="footer-link"><%= typeof t !== 'undefined' ? t('common.about') : '关于我们' %></a>
                <a href="<%= typeof localeUrl !== 'undefined' ? localeUrl('/contact') : '/contact' %>" class="footer-link"><%= typeof t !== 'undefined' ? t('common.contact') : '联系我们' %></a>
                <a href="<%= typeof localeUrl !== 'undefined' ? localeUrl('/privacy') : '/privacy' %>" class="footer-link"><%= typeof t !== 'undefined' ? t('common.privacy') : '隐私政策' %></a>
                <a href="<%= typeof localeUrl !== 'undefined' ? localeUrl('/terms') : '/terms' %>" class="footer-link"><%= typeof t !== 'undefined' ? t('common.terms') : '服务条款' %></a>
            </div>
            <div class="footer-version"><%= typeof t !== 'undefined' ? t('common.version') : '版本 1.0.0' %></div>
            <div class="footer-friendship-links">
                <span class="friendship-links-title"><%= typeof t !== 'undefined' ? t('common.friendshipLinks') : '友情链接：' %></span>
                <a href="https://mosaic.chdaoai.com/" target="_blank" rel="noopener noreferrer"><%= typeof t !== 'undefined' && t('common.friendLinks.mosaic') ? t('common.friendLinks.mosaic') : '免费图片处理' %></a>
                <span class="link-separator">|</span>
                <a href="https://www.removewatermarker.com/" target="_blank" rel="noopener noreferrer"><%= typeof t !== 'undefined' && t('common.friendLinks.videoWatermark') ? t('common.friendLinks.videoWatermark') : '视频去水印' %></a>
                <span class="link-separator">|</span>
                <a href="https://pdf.chdaoai.com/" target="_blank" rel="noopener noreferrer"><%= typeof t !== 'undefined' && t('common.friendLinks.pdfTool') ? t('common.friendLinks.pdfTool') : 'PDF转换工具' %></a>
                <span class="link-separator">|</span>
                <a href="https://qrcode.chdaoai.com/" target="_blank" rel="noopener noreferrer"><%= typeof t !== 'undefined' && t('common.friendLinks.qrcode') ? t('common.friendLinks.qrcode') : '在线生成二维码' %></a>
                <span class="link-separator">|</span>
                <a href="https://barcode.zorezoro.com/" target="_blank" rel="noopener noreferrer"><%= typeof t !== 'undefined' && t('common.friendLinks.barcode') ? t('common.friendLinks.barcode') : '在线生成条码' %></a>
                <span class="link-separator">|</span>
                <a href="https://www.zorezoro.com/" target="_blank" rel="noopener noreferrer"><%= typeof t !== 'undefined' && t('common.friendLinks.nbaFun') ? t('common.friendLinks.nbaFun') : 'NBA趣事' %></a>
                <span class="link-separator">|</span>
                <a href="https://video2txt.zorezoro.com/" target="_blank" rel="noopener noreferrer"><%= typeof t !== 'undefined' && t('common.friendLinks.video2txt') ? t('common.friendLinks.video2txt') : '视频转文字' %></a>
            </div>
            <div class="footer-copyright"><%= typeof t !== 'undefined' ? t('common.copyright') : '© 2024 Ice Breaker Games. All rights reserved.' %></div>
            <div class="footer-beian">
                <a href="https://beian.miit.gov.cn/" target="_blank" rel="noopener noreferrer">工业和信息化部 粤ICP备18041392号-6</a>
            </div>
        </div>
    </footer>

    <script>
        // 搜索功能（客户端增强）
        document.addEventListener('DOMContentLoaded', function() {
            const searchInput = document.getElementById('searchInput');
            const searchForm = searchInput?.closest('form');
            
            if (searchInput && searchForm) {
                let searchTimeout;
                searchInput.addEventListener('input', function() {
                    clearTimeout(searchTimeout);
                    const query = this.value.trim();
                    
                    if (query === '') {
                        return;
                    }
                    
                    // 防抖搜索
                    searchTimeout = setTimeout(() => {
                        window.location.href = '/search?q=' + encodeURIComponent(query);
                    }, 500);
                });
                
                searchForm.addEventListener('submit', function(e) {
                    e.preventDefault();
                    const query = searchInput.value.trim();
                    if (query) {
                        window.location.href = '/search?q=' + encodeURIComponent(query);
                    }
                });
            }
            
            // 导航链接点击时滚动到对应分类
            document.querySelectorAll('.nav-link').forEach(link => {
                link.addEventListener('click', function(e) {
                    const href = this.getAttribute('href');
                    if (href && href.startsWith('#')) {
                        e.preventDefault();
                        const targetId = href.substring(1);
                        const target = document.getElementById(targetId);
                        if (target) {
                            target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                            // 高亮显示
                            target.style.border = '2px solid rgba(102, 126, 234, 0.8)';
                            setTimeout(() => {
                                target.style.border = '1px solid rgba(255, 255, 255, 0.1)';
                            }, 2000);
                        }
                    }
                });
            });
        });
    </script>
</body>
</html>

